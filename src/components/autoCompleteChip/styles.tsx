import { primary } from "../../providers/styles/colors"

export const iconSx = {
    right: '5px',
    color: primary,
    fontSize: '22px',
    position: 'absolute',
    userSelect: 'none',
    display: 'inline-block',
    pointerEvents: 'none',
}